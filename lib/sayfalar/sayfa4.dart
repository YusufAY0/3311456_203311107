import 'package:flutter/material.dart';

class sayfadort extends StatelessWidget {
  const sayfadort({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("İNSAN"),
        centerTitle: true,
        backgroundColor: Colors.green,
      ),
    );
  }
}
