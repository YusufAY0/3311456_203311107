import 'package:flutter/material.dart';

class sayfabes extends StatelessWidget {
  const sayfabes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("İNSAN"),
        centerTitle: true,
        backgroundColor: Colors.green,
      ),
    );
  }
}
