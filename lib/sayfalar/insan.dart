import 'package:flutter/material.dart';

class insanSayfa extends StatelessWidget {
  const insanSayfa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("İNSAN"),
        centerTitle: true,
        backgroundColor: Colors.green,
      ),
    );
  }
}
